export const Loader: React.FC = () => {
  return (
    <div className="container">
      <span className="loader" data-testid="loader"></span>
    </div>
  );
};

// Loader component to indicate loading state in the application
// It displays a spinner to inform users that content is being loaded.
